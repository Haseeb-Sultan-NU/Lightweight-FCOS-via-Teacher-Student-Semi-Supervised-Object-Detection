# Dsl-object-detection
This repository implements a cross-architecture distillation strategy for semi-supervised object detection, where a Transformer-based teacher generates high-confidence pseudo-labels to train a lightweight CNN student under a weighted loss formulation.
